<%= form_for(vroumy) do |f| %>
  <% if vroumy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vroumy.errors.count, "error") %> prohibited this vroumy from being saved:</h2>

      <ul>
      <% vroumy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
