<%= simple_form_for(@constructeur) do |f| %>
  <%= f.error_notification %>

  <div class="row">
    <div class="form-group col-lg-5">
      <p>
    	<%= f.input :name, label:'Nom' %>
      </p>
    </div>
  </div>
  <div class="form-actions">
    <%= f.button :submit, value:'Enregistrer', class:'btn btn-success' %>
  </div>
<% end %>
