<%= simple_form_for(@voiture) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :couleur %>
    <%= f.input :description %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, label: 'Créer une voiture' %>
  </div>
<% end %>
